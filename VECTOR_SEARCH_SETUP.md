# ベクトル検索 (セマンティック検索) セットアップガイド

## 概要

ベクトル検索を有効にすると、アイテム名のあいまい検索や日本語での意味検索が可能になります。

例:
- 「速い戦闘機」→ 高速な船を検索
- 「シールド」→ Shield Generator 系アイテムを検索
- 「ピストル」→ FPS Pistol 系武器を検索

## 検索方式の比較

| 方式 | 依存 | 精度 | 構築時間 | 日本語 |
|------|------|------|----------|--------|
| LIKE (デフォルト) | なし | 完全一致のみ | 0秒 | 英語名のみ |
| FTS5 trigram | なし | 部分一致・typo耐性 | 数秒 | global.ini から自動マッピング |
| ベクトル検索 | エンベディングモデル | 意味的類似 | 約2分 | モデル依存（多言語対応） |

**FTS5 はインデックス構築時に自動で有効化されます。追加設定は不要です。**

## インデックス構築の所要時間（実測値）

設定タブの「インデックス構築」ボタンで以下が順に実行されます:

| ステップ | 内容 | 実測時間 | 備考 |
|----------|------|----------|------|
| 1/4 | アイテムインデックス構築 + 名前解決 | 約70秒 | 5,359件、27メーカー分のDCBクエリ + global.ini名前解決 |
| 2/4 | 船・車両データ抽出 | 約55秒 | 939件、19メーカー |
| 3/4 | ミッション・契約抽出 | 約13秒 | 1,600件 |
| 4/4 | コモディティ抽出 | 約4秒 | DCBクエリ |
| - | FTS5 再構築 + 日本語名マッピング | 数秒 | 自動実行 |
| **合計** | | **約2分半** | |

## ベクトル検索の有効化

### 方法1: Ollama (推奨 / ローカル / 無料)

1. Ollama をインストール: https://ollama.com/
2. エンベディングモデルをダウンロード:
   ```
   ollama pull nomic-embed-text
   ```
3. アプリの設定タブで「ベクトル検索構築」ボタンをクリック

**モデル情報:**
- `nomic-embed-text`: 768次元、多言語対応、軽量 (~270MB)
- ローカル実行のためAPI料金なし

**構築時間（実測値）:**
- 5,359 アイテム: **約2分15秒** (50件/バッチ、バッチあたり約1.3秒)
- バッチサイズ50件で処理、残り時間がリアルタイム表示されます

### 方法2: OpenAI

1. AI設定で OpenAI バックエンドを設定 (APIキー必須)
2. 「ベクトル検索構築」ボタンをクリック
3. 使用モデル: `text-embedding-3-small` (1536次元)

**予測時間:** 5,000 アイテムで約30秒
**コスト:** ~$0.001 (ほぼ無料)

### 方法3: Gemini

1. AI設定で Gemini バックエンドを設定 (APIキー必須)
2. 「ベクトル検索構築」ボタンをクリック
3. 使用モデル: `text-embedding-004` (768次元)

**予測時間:** 5,000 アイテムで約30〜60秒
**コスト:** 無料枠内

## 全体の構築フロー

「インデックス構築」→「ベクトル検索構築」の順で実行すると:

| フェーズ | 実測時間 |
|----------|----------|
| インデックス構築 | 約2分半 |
| ベクトル化 (Ollama) | 約2分15秒 |
| **総合計** | **約5分** |

## 検索の優先順位

チャットでアイテム検索を行った場合:

1. **FTS5 全文検索** (trigram) — 部分一致・日本語対応
2. **LIKE 検索** — FTS5 で結果が0件の場合のフォールバック
3. **ベクトル検索** — 構築済みの場合、セマンティック検索で補完 (将来対応予定)

## DB サイズへの影響

- FTS5 インデックス: +数MB
- ベクトルインデックス (768次元): 5,359件で約16MB
- ベクトルインデックス (1536次元): 5,359件で約32MB

## トラブルシューティング

### Ollama で `nomic-embed-text` が見つからない
```
ollama pull nomic-embed-text
```
を実行してモデルをダウンロードしてください。

### ベクトル構築が遅い
- Ollama: GPU が使われているか確認 (`ollama ps`)
- ネットワーク: OpenAI/Gemini はインターネット接続が必要

### 構築後にインデックスを再構築した場合
FTS5 は自動で再構築されますが、ベクトルインデックスは再構築が必要です。
「ベクトル検索構築」ボタンを再度クリックしてください。
