# VoiceVox セットアップガイド

VoiceVox を使うと、AI チャットの回答を音声で読み上げることができます。
Web チャット画面のスピーカーボタンをONにするだけで使えます。

## インストール

### 1. VoiceVox をダウンロード

公式サイト: https://voicevox.hiroshiba.jp/

- Windows 版をダウンロード (約 1GB)
- GPU 版 (NVIDIA CUDA) と CPU 版があります
  - GPU 版: 高速な音声合成 (NVIDIA GPU 必須)
  - CPU 版: どの PC でも動作

### 2. インストールと起動

1. ダウンロードしたインストーラーを実行
2. インストール完了後、VoiceVox を起動
3. 起動すると自動で **ポート 50021** でローカルサーバーが立ち上がります

> VoiceVox の GUI を閉じてもバックグラウンドでサーバーが動作します。
> タスクトレイにアイコンが表示されていればOKです。

### 3. 動作確認

ブラウザで以下にアクセスして、バージョン番号が表示されればOK:
```
http://localhost:50021/version
```

## アプリでの設定

### 設定タブ

| 設定項目 | デフォルト値 | 説明 |
|----------|-------------|------|
| VoiceVox URL | `http://localhost:50021` | VoiceVox サーバーのURL |
| VoiceVox 話者ID | `0` | 使用するキャラクター/スタイルのID |

### Web チャット画面

1. Web チャット画面を開く (`http://localhost:8099/`)
2. 右下のスピーカーボタン (&#128264;) をクリックして ON にする
3. AI の回答が自動で読み上げられます

### 話者 (キャラクター) の変更

1. Web チャット画面の歯車ボタンから設定を開く
2. 「VoiceVox 話者」ドロップダウンから好きなキャラクター/スタイルを選択
3. 「保存」をクリック

> VoiceVox が起動していれば、利用可能な全キャラクターが自動で一覧表示されます。

## 話者ID 一覧 (デフォルト)

| ID | キャラクター | スタイル |
|----|-------------|---------|
| 0 | 四国めたん | ノーマル |
| 1 | ずんだもん | ノーマル |
| 2 | 四国めたん | あまあま |
| 3 | ずんだもん | あまあま |
| 8 | 春日部つむぎ | ノーマル |
| 10 | 雨晴はう | ノーマル |
| 13 | 青山龍星 | ノーマル |
| 14 | 冥鳴ひまり | ノーマル |

> 全話者一覧: VoiceVox 起動中に `http://localhost:50021/speakers` で確認可能

## 音声入力 (マイク) について

Web チャット画面では Chrome の Web Speech API を使った音声入力が可能です。

### 操作方法

マイクボタン (&#127908;) の2つのモード:

| 操作 | 動作 |
|------|------|
| **タップ/クリック** | トグルモード: ON/OFF を切り替え。OFF にした時点で認識結果を送信 |
| **長押し (300ms以上)** | PTT モード: 押している間だけ録音。離すと認識結果を送信 |

### 対応ブラウザ

- **Chrome** (PC / Android): 対応
- **Safari** (iOS): 対応 (iOS 14.5+)
- **Firefox**: 非対応 (Web Speech API 未実装)
- **Edge**: 対応

### 言語設定

設定画面の「音声認識 言語」から切り替え可能:
- `ja-JP`: 日本語 (デフォルト)
- `en-US`: 英語 (Star Citizen の専門用語を直接入力したい場合)

## トラブルシューティング

### VoiceVox に接続できない

1. VoiceVox が起動しているか確認 (タスクトレイ)
2. ブラウザで `http://localhost:50021/version` にアクセスできるか確認
3. ファイアウォールでポート 50021 がブロックされていないか確認

### スマホから VoiceVox が使えない

VoiceVox はデフォルトで localhost のみ listen します。
スマホから使う場合は、VoiceVox の設定で listen アドレスを `0.0.0.0` に変更するか、
アプリの Web サーバー経由で VoiceVox にアクセスします (自動プロキシ対応)。

### 音声が途切れる / 遅い

- CPU 版を使っている場合、GPU 版への切り替えを検討
- 長い回答は自動で省略されます (500文字以上)
- VoiceVox の「スピード」設定を上げると高速化

### マイクが反応しない

1. ブラウザのマイク許可を確認 (アドレスバー左のアイコン)
2. Chrome の設定 > プライバシーとセキュリティ > サイトの設定 > マイク
3. http サイトの場合、Chrome フラグ `chrome://flags/#unsafely-treat-insecure-origin-as-secure` に `http://PC-IP:8099` を追加
